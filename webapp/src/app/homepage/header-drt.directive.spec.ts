import { HeaderDrtDirective } from './header-drt.directive';

describe('HeaderDrtDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderDrtDirective();
    expect(directive).toBeTruthy();
  });
});
