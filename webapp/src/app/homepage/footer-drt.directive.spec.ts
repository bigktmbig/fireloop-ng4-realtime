import { FooterDrtDirective } from './footer-drt.directive';

describe('FooterDrtDirective', () => {
  it('should create an instance', () => {
    const directive = new FooterDrtDirective();
    expect(directive).toBeTruthy();
  });
});
